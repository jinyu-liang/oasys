<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="TrainScoreMapper">
	
	<!--表名 -->
	<sql id="tableName">
		TB_TRAINSCORE
	</sql>
	
	<!-- 字段 -->
	<sql id="Field">
		TRAIN_ID,	
		SCORE_TYPE,	
		TRAINSCORE_ID
	</sql>
	
	<!-- 字段值 -->
	<sql id="FieldValue">
		#{TRAIN_ID},	
		#{SCORE_TYPE},	
		#{TRAINSCORE_ID}
	</sql>
	
	<!-- 新增-->
	<insert id="save" parameterType="pd">
		insert into 
	<include refid="tableName"></include>
		(
	<include refid="Field"></include>
		) values (
	<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 级联删除-->
	<delete id="delete" parameterType="pd">
		delete from
		<include refid="tableName"></include>
		where 
			TRAIN_ID = #{TRAINPLAN_ID}
	</delete>
	
	<!-- 修改 -->
	<update id="edit" parameterType="pd">
		update
		<include refid="tableName"></include>
		set 
			SCORE_TYPE = #{SCORE_TYPE},
		TRAINSCORE_ID = TRAINSCORE_ID
		where 
		TRAINSCORE_ID = #{TRAINSCORE_ID}
	</update>
	
	<!-- 通过培训ID获取数据 -->
	<select id="findById" parameterType="pd" resultType="pd">
		 select 
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 
			TRAIN_ID = #{PLAN_ID}
		
		<!-- select * 
		from TB_TRAINSCORE a JOIN TB_TRAINSCORES b ON a.TRAIN_ID=b.PLAN_ID 
		where TRAIN_ID = #{PLAN_ID} AND b.TRAINJOIN_ID=#{TRAINJOIN_ID} -->
		<!-- TRAIN_ID是培训的id  TRAINJOIN_ID是报名用户的id -->
	</select>
	
	<!-- 列表 -->
	<select id="datalistPage" parameterType="page" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
		where 1=1
		<if test="pd.keywords!= null and pd.keywords != ''"><!-- 关键词检索 -->
			and
				(
				<!--	根据需求自己加检索条件
					字段1 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%')
					 or 
					字段2 LIKE CONCAT(CONCAT('%', #{pd.keywords}),'%') 
				-->
				)
		</if>
	</select>
	
	<!-- 列表(全部) -->
	<select id="listAll" parameterType="pd" resultType="pd">
		select
		<include refid="Field"></include>
		from 
		<include refid="tableName"></include>
	</select>
	
	<!-- 批量级联删除 -->
	<delete id="deleteAll" parameterType="String">
		delete from
		<include refid="tableName"></include>
		where 
			TRAIN_ID in
		<foreach item="item" index="index" collection="array" open="(" separator="," close=")">
                 #{item}
		</foreach>
	</delete>
	
	<!-- zhangxuan5 -->
</mapper>